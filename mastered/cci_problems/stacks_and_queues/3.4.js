/*
	have 2 stacks.  Push pushes onto stack 1.  Pop checks if stack 2 is empty.  If so, it pushes everything in stack1 onto
	stack 2 except last element, which it pops off.  Else it simply pops off of stack 2.
*/

function queueViaStacks(){

}